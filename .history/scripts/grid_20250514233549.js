class Grid {
    constructor(canvas, rows, cols, cellSize) {
        this.canvas = canvas;
        this.rows = rows;
        this.cols = cols;
        this.cellSize = cellSize;
        this.space = space

    }
}